import {
    sliderReview,
    sliderWorks
} from './modules';

window.addEventListener('DOMContentLoaded', () => {
    console.log('GODNESS!!!');
    sliderWorks();
    // console.log(sliderReview);

});